name: sklearn
version: 1.0.0
kind: ecu

dependencies:
  - python3
  - python3-yaml
  - python3-sklearn

files:
  - run.py

entrypoint:
  kind: task
  exec: run.py

actions:
  'fit':
    # command:
    #   args:
    #     - pca
    input:
      - type: string
        name: input
        required: true
      - type: string
        name: n_components
        required: true
    output:
      - type: string
        name: output
